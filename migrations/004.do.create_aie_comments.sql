CREATE TABLE aie_comments (
    id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    text TEXT NOT NULL,
    date_created TIMESTAMP DEFAULT now() NOT NULL,
    article_id INTEGER
        REFERENCES aie_articles(id) ON DELETE CASCADE NOT NULL,
    user_id INTEGER
        REFERENCES aie_users(id) ON DELETE CASCADE NOT NULL
);
